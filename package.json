{
  "name": "css-grids",
  "version": "1.0.0",
  "description": "Demos for my talk",
  "keywords": ["css", "grids", "scss", "css-grids", "grid"],
  "author": "nzambello",
  "license": "MIT",
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^7.0.0",
    "live-server": "^1.2.0",
    "prettier": "^1.10.2",
    "stylelint": "^9.1.1",
    "stylelint-config-prettier": "^2.1.0",
    "stylelint-config-recommended": "^2.1.0"
  },
  "scripts": {
    "start": "yarn test && live-server --port=3000",
    "test": "stylelint */stylesheets/*.css",
    "prettier-all": "prettier --write '*/stylesheets/*.css'",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.{js,json,css}": ["prettier --write", "git add"]
  }
}
